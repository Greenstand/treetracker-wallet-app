apiVersion: v1
kind: Service
metadata:
  name: treetracker-wallet-web
spec:
  selector:
    app: treetracker-wallet-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3006
